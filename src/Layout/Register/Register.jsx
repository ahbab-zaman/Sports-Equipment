import Title from "../../Components/Title/Title";

const Register = () => {
    Title("Register")
    return (
        <div>
            this is register page
        </div>
    );
};

export default Register;